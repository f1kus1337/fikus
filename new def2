#new def back
    def back55():
        root3.destroy()
        top1.deiconify()

    button4 = Button(root3, text="⬅", command=back55, bg="black", fg="white", font=("Comic Sans MS", 9, "bold"),activebackground="black", width=5, height=1)
    button4.grid()
    button4.place(x=400, y=30)

#etc
